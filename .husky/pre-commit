#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged
npx lint-staged

# CrisisCore vector analysis
node scripts/crisis-core-audit.js --pre-commit
if [ $? -ne 0 ]; then
  echo "CrisisCore audit failed - fix critical vectors before committing"
  exit 1
fi
